print clock();
print clock;

fun count(n) {
  if (n > 1) count(n - 1);
  print n;
}

print count;
count(8);

fun sayHi(first, last) {
  print "Hi, " + first + " " + last + "!";
}

sayHi("Dear", "Reader");
